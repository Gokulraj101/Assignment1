package com.Prodapt.OOPbasics;

public class SimpleIntrest {
	static int p=2000,r=4,t=2,amount,SI;
	static void si()
	{
		SI=(p*r*t)/100;
		amount=SI+p;
		System.out.println("The simpleintrest is "+SI);
		amount=SI+p;
		System.out.println("Total amount is "+amount);
	}
}
